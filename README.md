# httpi

`httpi` is an HTTP client, CLI, and MCP project for sequencing HTTP requests in a modular, Git-tracked repo that works well for both humans and AI.

## Status

Scaffold only. Project structure and implementation will be added next.
