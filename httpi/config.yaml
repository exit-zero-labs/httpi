schemaVersion: 1
project: "httpi"
defaultEnv: dev

defaults:
  timeoutMs: 10000

capture:
  requestSummary: true
  responseMetadata: true
  responseBody: full
  maxBodyBytes: 1048576
  redactHeaders:
    - authorization
    - cookie
    - set-cookie
