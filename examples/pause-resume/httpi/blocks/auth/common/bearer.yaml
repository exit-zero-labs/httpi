# yaml-language-server: $schema=https://raw.githubusercontent.com/exit-zero-labs/httpi/main/packages/contracts/schemas/auth-block.schema.json
title: Bearer auth
auth:
  scheme: bearer
  token: "{{authToken}}"
